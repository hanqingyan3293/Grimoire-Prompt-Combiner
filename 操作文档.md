# 魔导书 Grimoire v7 — 操作文档

> 版本：v7.1.0 | 更新日期：2026-07-26

---

## 目录

1. [启动与界面概览](#1-启动与界面概览)
2. [标签库管理](#2-标签库管理)
3. [标签组管理](#3-标签组管理)
4. [收藏系统](#4-收藏系统)
5. [提示词组合器](#5-提示词组合器)
6. [预设管理](#6-预设管理)
7. [历史记录](#7-历史记录)
8. [AI 助手窗口](#8-ai-助手窗口)
9. [简易 AI 面板](#9-简易-ai-面板)
10. [AI 识图反推标签](#10-ai-识图反推标签)
11. [设置中心](#11-设置中心)
12. [快捷键管理](#12-快捷键管理)
13. [数据管理](#13-数据管理)
14. [常见问题](#14-常见问题)

---

## 1. 启动与界面概览

### 1.1 启动应用

- **安装版**：双击桌面快捷方式 `Grimoire`
- **免安装便携版**：下载 `Grimoire.7.1.0.exe` 后双击运行
- **源码版**：进入项目文件夹，双击 `启动.bat`。脚本会自动检查依赖、必要时构建，然后启动应用
- **生成便携版 EXE**：双击 `打包便携版.bat`，完成后到 `release/` 目录运行生成的便携版 EXE

### 1.2 主界面布局

启动后，主界面分为三大区域：

```
┌──────────┬──────────────────────────┬─────────────┐
│          │                          │             │
│  左侧栏   │      中间内容区           │   右侧栏     │
│  (标签库) │   (标签卡片/提示词输出)    │  (简易AI/    │
│          │                          │   预设/历史)  │
│          │                          │             │
└──────────┴──────────────────────────┴─────────────┘
```

| 区域 | 位置 | 功能 |
|------|------|------|
| 左侧栏 | 左侧固定 | 标签库树形浏览、标签组切换、收藏页、搜索 |
| 中间区 | 中央弹性 | 标签卡片网格、提示词输出框（正/负面）、随机按钮 |
| 右侧栏 | 右侧可折叠 | 简易AI聊天/识图、预设管理、历史记录、状态栏 |

### 1.3 右侧栏标签页

右侧栏顶部有 4 个标签页按钮：

| 标签 | 图标 | 功能 |
|------|------|------|
| 简易AI | 💬 | 内嵌AI聊天和识图面板 |
| 预设 | 📋 | 保存/加载提示词预设 |
| 历史 | 🕐 | 提示词生成历史 |
| 图片 | 🖼️ | 已添加的参考图片 |

---

## 2. 标签库管理

标签库是魔导书的核心，采用 **大类 → 子类 → 标签** 三层结构。

### 2.1 浏览标签

- 左侧栏显示所有大类（如「人物」「服饰」「场景」等）
- 点击大类名前的 **▶ 三角箭头** 展开，显示子类
- 点击子类名，**中间区域**显示该子类下的所有标签卡片
- 数字表示该层级下的标签数量：`大类名 (子类数/标签数)`

### 2.2 搜索标签

- 左侧栏顶部有 **🔍 搜索框**
- 输入关键词（中文或英文），实时过滤标签
- 匹配的标签会高亮显示

### 2.3 选中标签（添加到提示词）

| 操作 | 效果 |
|------|------|
| **左键点击子类** | 选中该子类下所有标签，右侧出现"子类批量选中"效果 |
| **左键点击标签** | 选中/取消选中单个标签 |
| **支持跨类多选** | 可同时选中不同大类下的子类和标签 |

选中后右侧会出现该子类的标签列表，可在输出框看到已选标签。

### 2.4 右键菜单

**右键标签：**

| 菜单项 | 功能 |
|--------|------|
| ✏️ 编辑 | 修改标签的中英文名称 |
| 🗑️ 删除 | 删除该标签（不可恢复） |
| ⭐ 收藏标签 | 添加到收藏页 |
| ➕ 加入正面 | 添加到正面提示词 |
| ➖ 加入负面 | 添加到负面提示词 |

**右键子类：**

| 菜单项 | 功能 |
|--------|------|
| ✏️ 编辑 | 修改子类名称 |
| 🗑️ 删除 | 删除子类及其下所有标签 |
| ⭐ 收藏子类 | 添加到收藏页 |

### 2.5 添加标签

- 左侧栏底部有 **➕ 添加** 按钮
- **添加大类**：输入大类中文名，自动生成英文名
- **添加子类**：选中大类后，输入子类名
- **添加标签**：选中子类后，输入标签中英文名

### 2.6 随机提示词

- 中间区底部有 **🎲 随机标签** 按钮
- 从当前选中的标签范围中随机抽取指定数量的标签
- 抽取数量可在设置中调整（默认范围可自定义）

### 2.7 提示词输出

中间区底部有固定的输出框，分为两行：

| 区域 | 边框颜色 | 用途 |
|------|----------|------|
| 正面提示词 | 无特殊边框 | 展示已选正面标签文本 |
| 负面提示词 | 无特殊边框 | 展示已选负面标签文本 |

输出框旁有 **📋 复制** 按钮，一键复制完整提示词。

---

## 3. 标签组管理

标签组是 **完整的独立标签数据库**，包含独立的大类/子类/标签体系。

### 3.1 使用场景

- 你有多个创作场景（如二次元、写实风、建筑），每个场景用不同的标签体系
- 别人分享了标签组给你，不想与现有标签混杂
- 需要备份当前标签库

### 3.2 切换标签组

1. 点击左侧栏顶部的 **🏷️ 标签库管理** 下拉菜单
2. 当前活跃的标签组前显示 `◉` 标记
3. 点击组名切换，中间区和左侧栏立即刷新

### 3.3 管理操作

在下拉菜单中：

| 操作 | 说明 |
|------|------|
| **新建组** | 点击 `+` 输入名称，创建空白标签组 |
| **重命名** | 右键组名 → 重命名 |
| **导入组** | 📥 导入他人分享的 `.json` 标签组文件 |
| **导出组** | 📤 导出当前组为 `.json` 文件 |
| **复制组** | 创建当前组的完整副本 |
| **设为默认** | 将当前组设为启动默认 |
| **删除组** | 删除整个组（不可恢复！） |

---

## 4. 收藏系统

### 4.1 收藏页入口

左侧栏底部 **⭐ 收藏** 按钮，切换到收藏页面。

### 4.2 收藏子类

- 收藏页上方区域为 **收藏的子类**
- 点击可查看该子类下的所有标签
- 再次点击取消选中
- 右键：编辑、取消收藏

### 4.3 收藏标签

- 收藏页下方区域为 **收藏的标签**
- 标签按 **大类 → 子类 → 标签** 三层树形结构展示
- 左键点击选中（支持跨类多选），再次点击取消
- 右键菜单与标签库相同（编辑、加入正/负面等）

---

## 5. 提示词组合器

### 5.1 正负面提示词

- 主界面右侧面板显示当前选中的 **正面提示词** 和 **负面提示词**
- 每个标签旁有 **权重滑块**（0.1 ~ 2.0），调整提示词强度

### 5.2 操作按钮

| 按钮 | 功能 |
|------|------|
| 🧹 清空 | 清除所有已选标签 |
| ↩️ 撤销 | 撤销上一步操作 |
| ↪️ 重做 | 重做已撤销的操作 |
| 📋 复制 | 复制完整提示词文本 |
| 💾 保存预设 | 保存当前组合为预设 |

---

## 6. 预设管理

### 6.1 保存预设

1. 组合好提示词后，点击 **💾 保存预设**
2. 输入预设名称
3. 预设保存在右侧栏「预设」标签页

### 6.2 加载预设

1. 切换到右侧栏「📋 预设」标签页
2. 点击预设名称加载
3. 提示词区域立即恢复保存时的状态

### 6.3 预设操作

- **重命名**：双击预设名称
- **删除**：点击预设旁的删除按钮

---

## 7. 历史记录

- 右侧栏「🕐 历史」标签页
- 每次操作（选中标签/生成提示词）自动记录
- 点击历史条目可查看详情
- 支持清空全部历史

---

## 8. AI 助手窗口

### 8.1 打开方式

- 主界面右侧栏 → 💬 简易AI → 点击 **🔲 独立窗口** 按钮
- 或通过菜单/快捷键打开

### 8.2 窗口布局

```
┌──────────┬──────────────────────────────────┐
│          │                                  │
│  对话列表 │         消息区域                   │
│  (分组)   │    (聊天气泡 + Markdown 渲染)      │
│          │                                  │
│  ─────── │ ──────────────────────────────── │
│ 设置按钮 │         输入区域                   │
│          │   [模型选择] [发送]               │
└──────────┴──────────────────────────────────┘
```

### 8.3 对话管理

**新建对话：**
- 点击对话列表顶部的 **➕ 新对话** 按钮
- 自动创建空白对话

**分组管理：**
- 左侧对话列表支持按 **时间** 或 **自定义分组** 组织对话
- 点击分组标题折叠/展开
- **右键分组** 可重命名、删除分组
- 拖拽对话到不同分组

**切换对话：**
- 点击左侧对话列表中的对话名称
- 消息区域加载该对话的完整历史

### 8.4 模型选择

- 输入框上方有 **模型下拉菜单**
- 列出当前供应商的所有可用模型
- 切换模型后立即生效

### 8.5 供应商切换

- 输入框旁有 **供应商切换** 按钮
- 显示当前活跃供应商名称
- 点击切换其他已配置的供应商

### 8.6 发送消息

- 在输入框输入内容
- 按 **Enter** 发送（可在设置中自定义）
- **Shift+Enter** 换行
- 点击 **发送按钮** 发送

### 8.7 消息气泡

| 角色 | 位置 | 样式 |
|------|------|------|
| 用户 | 右侧 | 蓝底白字，圆角气泡 |
| AI | 左侧 | 灰底边框，Markdown 渲染 |

### 8.8 消息功能

- **Markdown 渲染**：AI 回复支持代码高亮、表格、列表
- **消息持久化**：关闭窗口后重新打开，历史消息自动恢复
- **Token 估算**：底部显示当前对话的 Token 使用量

### 8.9 对话设置

点击输入框附近的 **⚙️ 对话设置** 按钮：

| 设置项 | 说明 |
|--------|------|
| 系统提示词 | 自定义 AI 的系统提示 |
| 自动压缩上下文 | 开启后自动管理上下文长度 |
| 温度参数 | 控制 AI 回复的随机性 |

### 8.10 AI 刷新按钮

- 工具栏上的 **🔄 刷新** 按钮
- 同步全局 AI 状态，包括模型列表、供应商配置等
- 当 AI 响应异常时，可尝试刷新

---

## 9. 简易 AI 面板

### 9.1 位置

主界面右侧栏的第一个标签页（💬 简易AI）。

### 9.2 双模式

| 模式 | 用途 |
|------|------|
| 💬 聊天 | 快速 AI 对话，与 AI 助手窗口同步 |
| 🔍 识图 | 上传图片进行 AI 识图反推标签 |

### 9.3 与 AI 窗口同步

- 简易面板和 AI 助手窗口 **共享对话数据**
- 在一处发送的消息，另一处可见
- 新建对话同步出现在两边

### 9.4 模型选择

- 简易面板有自己的模型下拉菜单
- 切换模型后与 AI 窗口同步

---

## 10. AI 识图反推标签

### 10.1 入口

- 简易AI面板 → **🔍 识图** 标签
- AI助手窗口 → 顶部 **识图** 标签

### 10.2 上传图片

| 方式 | 操作 |
|------|------|
| 点击上传 | 点击上传区域，打开文件选择器 |
| 拖拽上传 | 将图片文件拖入上传区域 |
| 多选 | 文件选择器支持 Ctrl+多选 |

### 10.3 识图流程

1. 上传图片 → 预览显示
2. 选择 AI 模型（自动过滤不支持多模态的模型）
3. 点击 **🔍 开始识图** 按钮
4. AI 分析图片内容，返回推荐标签列表

### 10.4 识图结果操作

AI 返回标签建议后，你可以：

| 操作 | 按钮 | 说明 |
|------|------|------|
| 勾选/取消 | ☑️ 复选框 | 选择需要的标签 |
| 复制 | 📋 复制 | 复制选中标签文本 |
| 添加到正面 | ➕ 正面 | 添加到正面提示词 |
| 收藏 | ⭐ 收藏 | 添加到收藏页 |
| 导入到标签库 | 📥 导入 | 选择大类/子类保存标签 |
| 历史记录 | — | 自动保存识图历史 |

### 10.5 识图历史

- 识图结果自动保存
- 可在历史中查看之前的识图记录
- 与 AI 窗口的识图历史同步

---

## 11. 设置中心

### 11.1 打开设置

- 点击主界面状态栏的 **⚙ 设置** 按钮
- 设置窗口以 **独立窗口** 打开，可自由切换回主界面

### 11.2 设置分类

左侧导航栏有 6 个分类：

| 分类 | 图标 | 内容 |
|------|------|------|
| 通用 | ⚙ | 语言、字体大小、随机标签数范围 |
| 外观 | 🎨 | 主题选择、自定义强调色 |
| API 配置 | 🔌 | 供应商管理（核心设置） |
| 快捷键 | ⌨ | 全局快捷键自定义 |
| 数据 | 💾 | 数据库导入/导出 |
| 关于 | ℹ | 版本信息 |

### 11.3 通用设置

| 设置项 | 选项 | 说明 |
|--------|------|------|
| 语言 | 中文 / English | 界面语言 |
| 字体大小 | 小 / 中 / 大 | 全局字体和UI缩放 |
| 随机标签数范围 | 数字输入 | 控制随机抽取标签的数量范围 |

> 💡 提示：由于多窗口限制，若设置或历史记录未同步，请手动按 Ctrl+R 刷新页面。

### 11.4 外观设置

| 设置项 | 说明 |
|--------|------|
| 主题 | 7套内置主题：霓虹(紫)、简洁(蓝)、金色(黄)、暗夜(靛)、樱花(粉)、森林(绿)、日落(橙) |
| 自定义强调色 | 点击色块打开取色器，自定义主题色 |

### 11.5 API 配置（供应商管理）

这是最重要的设置页面，配置 AI 通信。

**供应商列表：**
- 显示所有已配置的 API 供应商
- 当前活跃供应商标记为绿色高亮

**新增/编辑供应商：**

| 配置项 | 说明 |
|--------|------|
| 名称 | 供应商显示名称（如 "OpenAI"、"DeepSeek"） |
| 接入模式 | `官方登录` 或 `纯 API` |
| 上游协议 | `Responses API` 或 `Chat Completions` |
| 默认配置模型 | AI 聊天默认使用的模型 |
| 测试模型 | 测试连接时使用的模型 |
| 上下文大小 | Token 上限（留空使用默认值） |
| Base URL | API 接口地址 |
| API Key | 你的 API 密钥（加密存储） |
| 模型列表 | 从上游获取或手动填写可用模型 |

**配置预览：**
- `config.toml 预览`：通用配置文件，**可直接编辑**
- `auth.json 预览`：认证配置文件，**可直接编辑**

**操作按钮：**

| 按钮 | 功能 |
|------|------|
| 🔄 从上游获取模型 | 自动检测 API Key 可用模型，勾选添加 |
| 🧪 测试连接 | 验证 API 配置是否正确 |
| 💾 保存 | 保存供应商配置 |
| 🔘 切换 | 切换当前活跃供应商 |
| 🗑️ 删除 | 删除供应商配置 |

**从上游获取模型：**
1. 填写 Base URL 和 API Key
2. 点击「从上游获取模型」
3. 系统自动请求 `/models` 接口
4. 显示可用模型列表
5. **勾选**需要的模型 → 确认添加
6. 勾选的模型出现在 AI 助手下拉菜单中

---

## 12. 快捷键管理

### 12.1 默认快捷键

| 功能 | 默认快捷键 |
|------|-----------|
| 发送消息 | Enter |
| 换行 | Shift+Enter |
| 搜索 | Ctrl+F |
| 撤销 | Ctrl+Z |
| 重做 | Ctrl+Shift+Z |
| 复制 | Ctrl+C |
| 关闭窗口 | Escape |

### 12.2 自定义快捷键

1. 打开设置 → **⌨ 快捷键**
2. 点击要修改的快捷键
3. 按下新的组合键
4. 自动保存

- **清除快捷键**：点击 `✕ 清除` 按钮，该功能不再有快捷键
- 支持组合：Ctrl / Shift / Alt + 字母键

---

## 13. 数据管理

### 13.1 数据库位置

- 默认路径：`%APPDATA%/grimoire/grimoire.db`
- 包含：标签数据、API 配置（加密）、聊天记录、收藏、预设等

### 13.2 备份与恢复

打开设置 → **💾 数据**：

| 按钮 | 功能 |
|------|------|
| 📤 导出数据库 | 将数据库导出为 `.db` 文件 |
| 📥 导入数据库 | 从 `.db` 文件恢复数据 |

### 13.3 便携版数据

便携版的所有数据保存在应用所在文件夹内，无需额外备份。压缩整个文件夹即可迁移。

---

## 14. 常见问题

### Q: AI 助手发消息没反应？
1. 确认设置中已配置 API 供应商
2. 点击「测试连接」确认配置正确
3. 点击 🔄 刷新按钮同步状态
4. 检查模型下拉菜单是否已选择模型

### Q: 简易AI和AI窗口不同步？
- 手动按 **Ctrl+R** 刷新页面
- 切换右侧栏标签页会自动触发刷新

### Q: 识图功能报错？
- 确认选择的支持多模态的模型（如 GPT-4o、GPT-4 Vision 等）
- 检查 API Key 是否有相应权限

### Q: 标签库数据丢失？
- 使用设置中的 📥 导入数据库恢复
- 或从标签组管理导入备份的标签组

### Q: 如何添加新的 API 供应商？
1. 打开设置 → API 配置
2. 点击「新增供应商」
3. 填写供应商信息
4. 点击「从上游获取模型」获取可用模型列表
5. 勾选需要的模型
6. 保存 → 切换为活跃供应商

### Q: 窗口一闪而过无法启动？
1. 确保已执行 `npm run build` 构建
2. 检查 `dist/` 目录是否存在
3. 重新构建：`npm run build && npm start`

---

*魔导书 Grimoire v7 — GPL-3.0 License*
